version: '3.8'

services:
  prompt-builder:
    image: smkgun/promptly:latest
    user: root
    container_name: prompt-builder
    ports:
      - "8080:3001"
    volumes:
      - /mnt/cyano/appdata/promptly/data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3001
      - DATA_DIR=/app/data
      - NODE_ENV=production
      - JWT_SECRET=mtz+dbyg9g/LW3PkQ4WvJOhHYXtwVDaETRRrCaEWN9k=
    deploy:
      resources:
        limits:
          memory: 512M
