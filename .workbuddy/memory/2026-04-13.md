# 2026-04-13 工作日志

## ddnsto + WebDAV 认证方案咨询
- 分析了 ddnsto 穿透 + NAS WebDAV 的双层认证问题
- 第1层：ddnsto IP验证（未验证IP被拦截返回HTML），第2层：WebDAV Basic Auth
- 提出5种方案：浏览器预认证、关闭ddnsto验证、内嵌窗口认证、本地代理、换穿透工具
- 推荐分阶段实施：先试关闭验证/预认证，后续优化体验
- 用户尚未选择具体方案

## Prompt Builder Docker 部署改造
- 已完成全部文件：Dockerfile(多阶段构建+非root用户+tini)、docker-compose.yml(内存限制128M)、.dockerignore、server.js(Express API)、DEPLOY.md(完整部署指南)
- 部署方案：Vite build → Express 同时托管静态文件 + API → 数据存 /app/data JSON 文件
- 用户目标：部署到 OpenWrt 路由器 Docker，需注意架构匹配（ARM64/ARM32/x86）
- 本机无 Docker Desktop，镜像需在目标设备上构建或交叉构建后导入
- 修复了颜色面板选择失效的 bug：全局遮挡层 onMouseDown 和颜色按钮 onClick 事件竞争，改为 onClick + 调整 z-index 层级
- 新增多用户账号功能：开放注册、JWT Token 认证、数据隔离（每用户独立目录）、修改密码、退出登录
- 新增 GitHub Actions 自动构建：推送 main 分支自动构建 amd64+arm64 双架构镜像并推送到 DockerHub
- 本机无 Docker，采用 GitHub Actions 方案代替本地构建
- 已创建 .gitignore、.github/workflows/docker-build.yml、DEPLOY.md 部署指南
